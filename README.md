For Mac Users about running virtual environment:
```bash
python3 -m venv myvenv
source myvenv/bin/activate
```
and download all the requirement on requirement.txt to run the project
```bash
pip install -r requirement.txt
```
